# 10_colors

10_colors.gad — a partial of main.gad's glob include `include ("parts/*.gad")`;
the numeric prefix fixes its place in the (path-sorted) include order.

## Example — `10_colors.gad`

```gad
palette := ["red", "green"]
```
