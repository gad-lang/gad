# 30_draft

30_draft.gad — a work-in-progress partial that main.gad's glob include leaves
out with `excludes=["*_draft.gad"]` (it would reset `swatches`).

## Example — `30_draft.gad`

```gad
swatches = 0
```
